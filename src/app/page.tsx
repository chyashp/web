import HomeClient from "@/components/HomeClient";

export default function Home() {
  return (
    <main className="bg-gradient-to-b from-primary-50 via-orange-50/30 to-white min-h-screen">
      <HomeClient />
    </main>
  );
}
